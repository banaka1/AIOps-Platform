# 智能运维 Agent 平台 · 操作说明书

> 本说明书面向**非技术人员**，按步骤操作即可完成部署和使用。遇到问题请查阅文末「常见问题排查」。

## 1. 环境准备

### 1.1 需要安装的软件

| 软件 | 下载地址 | 说明 |
|---|---|---|
| **Python** | https://www.python.org/downloads/ | 选 3.9 或更高版本，安装时勾选 **Add Python to PATH** |
| **Node.js** | https://nodejs.org/ | 选 LTS 版本即可 |
| **MySQL** | https://dev.mysql.com/downloads/mysql/ | 5.7 或 8.0，安装时记住 root 密码 |
| **浏览器** | Chrome / Edge | 访问系统用 |

### 1.2 需要申请的 API Key（免费）

| 服务 | 申请地址 | 用途 |
|---|---|---|
| **硅基流动** | https://cloud.siliconflow.cn/ | AI 对话 + 语义检索，注册送免费额度 |
| **高德开放平台** | https://console.amap.com/ | 天气查询，申请「Web服务」类型 Key |

---

## 2. 环境配置

### 2.1 创建 .env 配置文件

在项目根目录（`MachineLearning/` 文件夹下）新建一个文本文件，命名为 `.env`（注意没有后缀名），复制以下内容并填入你自己的 Key：

```ini
# ============ AI 对话（硅基流动）============
SILICONFLOW_API_KEY=你的硅基流动Key
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL_NAME=deepseek-ai/DeepSeek-V4-Pro

# ============ 天气查询（高德）============
AMAP_API_KEY=你的高德Key
AMAP_WEATHER_URL=https://restapi.amap.com/v3/weather/weatherInfo

# ============ MySQL 数据库 ============
MYSQL_DSN=mysql+pymysql://root:你的MySQL密码@127.0.0.1:3306/agent_ops?charset=utf8mb4

# ============ 登录密钥 ============
JWT_SECRET=随便写一串随机字符作为密钥
JWT_EXP_DAYS=7
```

> ⚠️ 前 5 项（硅基流动 3 项 + 高德 2 项 + MySQL）必须填写，否则后端无法启动。

### 2.2 安装后端依赖

打开命令行（Windows 按 `Win+R` 输入 `cmd` 回车），进入项目目录后执行：

```bash
pip install fastapi uvicorn sqlalchemy pymysql python-jose[cryptography] bcrypt python-dotenv langchain langchain-openai requests sqlparse chromadb langchain-chroma
```

> 如果 `pip` 命令找不到，说明 Python 安装时没勾选 PATH，重新安装 Python 并勾选即可。

### 2.3 安装前端依赖

```bash
cd frontend
npm install
```

等待安装完成（可能需要几分钟）。

---

## 3. 数据库初始化

### 3.1 创建 MySQL 数据库

打开 MySQL 命令行（或 Navicat 等工具），执行项目 `sql/` 目录下的 `init.sql` 脚本。

命令行方式：
```bash
mysql -u root -p < sql/init.sql
```

该脚本会自动创建 `agent_ops` 数据库及所需表。

### 3.2 生成示例数据和向量索引

在项目根目录依次执行：

```bash
# 1. 生成运维示例数据库（服务器/指标/告警数据）
python init_ops_db.py

# 2. 构建故障诊断的语义检索索引（让 AI 能理解模糊的故障描述）
python build_fault_kb_vector.py
```

两条命令都看到 `✅` 提示就说明成功了。

---

## 4. 启动服务

### 4.1 启动后端

打开一个命令行窗口，在项目根目录执行：

```bash
uvicorn controller:app --host 127.0.0.1 --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 就说明后端启动成功。**这个窗口不要关闭**。

### 4.2 启动前端

**再打开一个新的**命令行窗口，执行：

```bash
cd frontend
npm run dev
```

看到 `Local: http://127.0.0.1:5173/` 就说明前端启动成功。**这个窗口也不要关闭**。

---

## 5. 使用系统

### 5.1 注册账号

1. 浏览器打开 `http://127.0.0.1:5173`
2. 点击「注册」，输入用户名和密码
3. 注册成功后自动跳转登录页

### 5.2 登录使用

1. 输入刚注册的用户名密码登录
2. 进入聊天界面，左侧是会话列表，右侧是聊天区
3. 顶部「可查询数据表」展示了可以查询的数据表结构

### 5.3 你可以这样问 AI

| 想做什么 | 输入示例 |
|---|---|
| 查天气 | `北京今天天气怎么样？` |
| 查服务器 | `CPU 使用率最高的服务器是哪台？` |
| 查告警 | `列出所有严重告警` |
| 写 SQL | `帮我写一个查询服务器 CPU 和内存的 SQL` |
| 排查故障 | `服务器 CPU 飙高了怎么办？` |
| 模糊故障 | `机器跑不动了进程都被杀了`（AI 能理解是内存问题） |

### 5.4 其他操作

- **新建对话**：点击左侧「+ 新对话」
- **切换对话**：点击左侧会话列表
- **删除对话**：鼠标悬停在会话上，点垃圾桶图标
- **发送消息**：输入框输入后按 `Enter`，`Shift+Enter` 换行
- **退出登录**：点击右上角用户名 →「退出登录」

---

## 6. 停止服务

直接关闭两个命令行窗口即可（后端和前端各一个）。

---

## 7. 常见问题排查

### 7.1 后端启动报错 `Missing required env vars`

`.env` 文件没创建或缺少必填项，回到「2.1 节」检查前 5 项是否都填了。

### 7.2 后端启动报错 `ModuleNotFoundError`

依赖没装全，重新执行「2.2 节」的 `pip install` 命令。

### 7.3 前端页面打不开

确认前端命令行窗口还开着，且显示 `Local: http://127.0.0.1:5173/`。

### 7.4 登录后发消息没反应

确认后端命令行窗口还开着，且显示 `Uvicorn running on http://127.0.0.1:8000`。

### 7.5 天气查询返回「未配置 AMAP_API_KEY」

`.env` 里高德 Key 没填或填错了，检查「2.1 节」。

### 7.6 查数据报「no such table」

示例数据库没生成，执行 `python init_ops_db.py`。

### 7.7 故障诊断答非所问

向量索引没构建，执行 `python build_fault_kb_vector.py`。若报错检查硅基流动 Key 是否有效。

### 7.8 端口 8000 / 5173 被占用

关掉占用端口的程序，或重启电脑后重新启动服务。

---

## 8. 生产部署（供技术人员参考）

### 后端

```bash
uvicorn controller:app --host 0.0.0.0 --port 8000
```

### 前端

```bash
cd frontend
npm run build
# 将 dist/ 目录部署到 Nginx 或静态服务器
```

### Nginx 反向代理示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```
